import { Module } from '@nestjs/common';

// Placeholder module — TypeORM is configured globally in AppModule
// This module can be extended with migration utilities if needed
@Module({})
export class DatabaseModule {}
